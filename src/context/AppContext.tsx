import React,{createContext,useContext,useEffect,useMemo,useState} from 'react';
import {initialRequests} from '../data/mockData';
import {AppAlert,AuthSession,CancellationReason,Complaint,Message,Provider,ProviderAccountStatus,ProviderAvailability,Request,RequestInput,Review,Role,Service,ServicePricing,UserProfile} from '../types';
import {loadAlerts,loadAvailability,loadComplaints,loadFavourites,loadMessages,loadProfile,loadProviderStatuses,loadProviders,loadRequests,loadReviews,loadServices,loadSession,saveAlerts,saveAvailability,saveComplaints,saveFavourites,saveMessages,saveProfile,saveProviderStatuses,saveProviders,saveRequests,saveReviews,saveServices,saveSession} from '../services/storage';
import {providers as seedProviders,services as seedServices} from '../data/mockData';
type CompletionProofInput={notes:string;photos:string[]};
type AlertInput=Omit<AppAlert,'id'|'createdAt'|'read'> & {read?:boolean};
type MessageMeta={requestId?:string;title?:string;avatar?:string;recipientRoles?:Role[]};
type Context={role:Role;setRole:(role:Role)=>void;authenticated:boolean;login:(email:string,password:string)=>boolean;logout:()=>void;requests:Request[];addRequest:(r:Request)=>void;createRequest:(r:RequestInput)=>Request;updateRequest:(id:string,status:Request['status'])=>void;verifyArrival:(id:string)=>boolean;submitCompletionProof:(id:string,proof:CompletionProofInput)=>boolean;submitQuote:(id:string,pricing:Omit<ServicePricing,'total'|'platformFee'|'providerEarnings'>)=>boolean;approveQuote:(id:string)=>boolean;rejectQuote:(id:string)=>boolean;canCancelRequest:(request:Request)=>boolean;cancelRequest:(id:string,reason?:CancellationReason)=>boolean;removeRequest:(id:string)=>void;favourites:string[];toggleFavourite:(id:string)=>void;messages:Message[];sendMessage:(providerId:string,text:string,meta?:MessageMeta)=>void;markMessageRead:(id:string)=>void;alerts:AppAlert[];pushAlert:(alert:AlertInput)=>void;markAlertRead:(id:string)=>void;markAllAlertsRead:()=>void;reviews:Review[];addReview:(review:Review)=>void;hasReview:(requestId:string)=>boolean;complaints:Complaint[];addComplaint:(complaint:Complaint)=>void;providerDirectory:Provider[];addProvider:(provider:Provider)=>void;serviceDirectory:Service[];addService:(service:Service)=>void;providerStatuses:Record<string,ProviderAccountStatus>;setProviderStatus:(id:string,status:ProviderAccountStatus)=>void;availability:ProviderAvailability;setAvailability:(id:string,value:boolean)=>void;profile:UserProfile;updateProfile:(patch:Partial<UserProfile>)=>void;ready:boolean};
const noop=()=>{};
const AppContext=createContext<Context>({role:'customer',setRole:noop,authenticated:false,login:()=>false,logout:noop,requests:[],addRequest:noop,createRequest:(r)=>({...r,id:'',status:r.status||'Pending'} as Request),updateRequest:noop,verifyArrival:()=>false,submitCompletionProof:()=>false,submitQuote:()=>false,approveQuote:()=>false,rejectQuote:()=>false,canCancelRequest:()=>false,cancelRequest:()=>false,removeRequest:noop,favourites:[],toggleFavourite:noop,messages:[],sendMessage:noop,markMessageRead:noop,alerts:[],pushAlert:noop,markAlertRead:noop,markAllAlertsRead:noop,reviews:[],addReview:noop,hasReview:()=>false,complaints:[],addComplaint:noop,providerDirectory:[],addProvider:noop,serviceDirectory:[],addService:noop,providerStatuses:{},setProviderStatus:noop,availability:{},setAvailability:noop,profile:{name:'Alex Morgan',email:'alex@example.com',phone:'',address:'12 Long Street, Cape Town',notifications:true,role:'customer'},updateProfile:noop,ready:false});
const accounts:{email:string;password:string;role:Role;name:string}[]=[{email:'customer@fixmate.app',password:'fixmate123',role:'customer',name:'Alex Morgan'},{email:'provider@fixmate.app',password:'fixmate123',role:'provider',name:'Thabo Mokoena'},{email:'admin@fixmate.app',password:'fixmate123',role:'administrator',name:'FixMate Admin'},{email:'system@fixmate.app',password:'fixmate123',role:'system-admin',name:'System Admin'}];
const defaultMessages:Message[]=[{id:'m1',providerId:'p1',provider:'Thabo Mokoena',avatar:'TM',text:'I’ll be there at 2pm. Please keep the geyser area clear.',timestamp:'10:42',unread:true,senderRole:'provider',senderName:'Thabo Mokoena',recipientRoles:['customer'],unreadFor:['customer']}];
const allowedTransitions:Record<Request['status'],Request['status'][]>={Searching:['Pending','Cancelled'],Pending:['Accepted','Cancelled'],Accepted:['Arrived','Cancelled'],Arrived:['In progress','Cancelled'],['In progress']:['Completed','Cancelled'],Completed:[],Cancelled:[]};
export function AppProvider({children}:{children:React.ReactNode}){const [role,setRoleState]=useState<Role>('customer');const [requests,setRequests]=useState<Request[]>(initialRequests);const [favourites,setFavourites]=useState<string[]>([]);const [messages,setMessages]=useState<Message[]>(defaultMessages);const [alerts,setAlerts]=useState<AppAlert[]>([]);const [reviews,setReviews]=useState<Review[]>([]);const [complaints,setComplaints]=useState<Complaint[]>([]);const [providerDirectory,setProviderDirectory]=useState<Provider[]>(seedProviders);const [serviceDirectory,setServiceDirectory]=useState<Service[]>(seedServices);const [providerStatuses,setProviderStatuses]=useState<Record<string,ProviderAccountStatus>>({p1:'approved',p2:'approved',p3:'approved',p4:'pending'});const [availability,setAvailabilityState]=useState<ProviderAvailability>({p1:true,p2:true,p3:true,p4:false});const [profile,setProfile]=useState<UserProfile>({name:'Alex Morgan',email:'alex@example.com',phone:'',address:'12 Long Street, Cape Town',notifications:true,role:'customer'});const [ready,setReady]=useState(false);const [authenticated,setAuthenticated]=useState(false);
 useEffect(()=>{Promise.all([loadRequests(),loadFavourites(),loadMessages(),loadAlerts(),loadReviews(),loadComplaints(),loadProviders(),loadServices(),loadProfile(),loadProviderStatuses(),loadAvailability(),loadSession()]).then(([r,f,m,n,v,c,storedProviders,storedServices,p,ps,a,session])=>{if(r.length)setRequests(r);setFavourites(f);if(m.length)setMessages(m);if(n.length)setAlerts(n);setReviews(v);setComplaints(c);if(storedProviders.length)setProviderDirectory(storedProviders);if(storedServices.length)setServiceDirectory(storedServices);setProfile(p);setRoleState(p.role);setProviderStatuses(ps);setAvailabilityState(a);if(session){setAuthenticated(true);setRoleState(session.role);setProfile(current=>({...current,email:session.email,role:session.role}));}setReady(true);});},[]);
 useEffect(()=>{if(!ready)return;saveRequests(requests);saveFavourites(favourites);saveMessages(messages);saveAlerts(alerts);saveReviews(reviews);saveComplaints(complaints);saveProviders(providerDirectory);saveServices(serviceDirectory);saveProfile(profile);saveProviderStatuses(providerStatuses);saveAvailability(availability);},[requests,favourites,messages,alerts,reviews,complaints,providerDirectory,serviceDirectory,profile,providerStatuses,availability,ready]);
 const login=(email:string,password:string)=>{const account=accounts.find(item=>item.email.toLowerCase()===email.trim().toLowerCase()&&item.password===password);if(!account)return false;const session:AuthSession={email:account.email,role:account.role};setAuthenticated(true);setRoleState(account.role);setProfile(current=>({...current,email:account.email,name:account.name,role:account.role}));saveSession(session);return true;};
 const logout=()=>{setAuthenticated(false);saveSession(null);};
 const setRole=(value:Role)=>{setRoleState(value);setProfile(p=>({...p,role:value}));};
 const pushAlert=(alert:AlertInput)=>setAlerts(current=>[{...alert,id:`alert_${Date.now()}_${current.length}`,createdAt:new Date().toISOString(),read:alert.read||false},...current].slice(0,80));
 const markAlertRead=(id:string)=>setAlerts(current=>current.map(alert=>alert.id===id?{...alert,read:true}:alert));
 const markAllAlertsRead=()=>setAlerts(current=>current.map(alert=>alert.recipientRoles.includes(role)?{...alert,read:true}:alert));
 const notify=(recipientRoles:Role[],title:string,text:string,type:AppAlert['type']='system',requestId?:string)=>pushAlert({recipientRoles,title,text,type,requestId});
 const addRequest=(request:Request)=>{setRequests(current=>[{...request,createdAt:request.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString(),paymentStatus:request.paymentStatus||'unpaid',arrivalStatus:request.arrivalStatus||'not_started',providerDistanceKm:request.providerDistanceKm??2.4},...current]);notify(['provider','administrator','system-admin'],'New job request',`${request.customer||'A customer'} requested ${request.service}.`,'booking',request.id);};
 const createRequest=(input:RequestInput)=>{const now=new Date().toISOString();const request={...input,id:`req_${Date.now()}`,status:input.status||'Searching',createdAt:now,updatedAt:now,paymentStatus:input.paymentStatus||'unpaid',arrivalStatus:input.arrivalStatus||'not_started',providerDistanceKm:input.providerDistanceKm??2.4} as Request;setRequests(current=>[request,...current]);notify(['provider','administrator','system-admin'],'New job request',`${request.customer||'A customer'} requested ${request.service}.`,'booking',request.id);return request;};
 const updateRequest=(id:string,status:Request['status'])=>setRequests(current=>current.map(request=>{const allowed=allowedTransitions[request.status].includes(status);const arrivalRequired=request.status==='Accepted'&&status==='Arrived'&&request.quoteStatus!=='approved';const startRequired=request.status==='Arrived'&&status==='In progress'&&request.arrivalStatus!=='arrived';const quoteRequired=request.status==='In progress'&&status==='Completed'&&request.quoteStatus!=='approved';const proofRequired=request.status==='In progress'&&status==='Completed'&&request.completionProofStatus!=='submitted'&&request.completionProofStatus!=='accepted';if(request.id!==id||!allowed||arrivalRequired||startRequired||quoteRequired||proofRequired)return request;return {...request,status,completionProofStatus:status==='Completed'?'accepted':request.completionProofStatus,completedAt:status==='Completed'?new Date().toISOString():request.completedAt,paymentStatus:status==='Completed'&&request.quoteStatus==='approved'?'paid':request.paymentStatus,updatedAt:new Date().toISOString()};}));
 const verifyArrival=(id:string)=>{const target=requests.find(request=>request.id===id);if(!target||target.status!=='Accepted'||target.quoteStatus!=='approved')return false;const distanceMeters=65;setRequests(current=>current.map(request=>request.id===id?{...request,status:'Arrived',arrivalStatus:'arrived',arrivedAt:new Date().toISOString(),arrivalDistanceMeters:distanceMeters,providerDistanceKm:0.1,estimatedArrivalMinutes:0,updatedAt:new Date().toISOString()}:request));notify(['customer'],'Provider arrived',`${target.provider} has arrived for ${target.service}.`,'arrival',id);return true;};
 const submitCompletionProof=(id:string,proof:CompletionProofInput)=>{const notes=proof.notes.trim();const photos=proof.photos.filter(Boolean);const target=requests.find(request=>request.id===id);if(!notes||photos.length===0||!target||target.status!=='In progress'||target.quoteStatus!=='approved')return false;const now=new Date().toISOString();setRequests(current=>current.map(request=>request.id===id?{...request,status:'Completed',completionNotes:notes,completionPhotos:photos,completionProofStatus:'submitted',completedAt:now,paymentStatus:'paid',updatedAt:now}:request));notify(['customer','administrator'],'Job completed',`${target.provider} submitted completion proof for ${target.service}.`,'payment',id);return true;};
 const submitQuote=(id:string,input:Omit<ServicePricing,'total'|'platformFee'|'providerEarnings'>)=>{const target=requests.find(request=>request.id===id);if(!target||!['Pending','Accepted','In progress'].includes(target.status))return false;const billingModel=input.billingModel||'hourly';const billableHours=billingModel==='hourly'?Math.max(input.labourHours||0,input.minimumHours||1):input.labourHours||1;const labour=billingModel==='hourly'?Math.round((input.labourRate||0)*billableHours):input.labour;const total=input.callOut+labour+input.materials+input.extras;const platformFee=Math.round(total*.15);const pricing={...input,billingModel,labour,billableHours,total,platformFee,providerEarnings:total-platformFee,submittedAt:new Date().toISOString()};setRequests(current=>current.map(request=>request.id===id?{...request,pricing,quoteStatus:'pending_approval',paymentStatus:'unpaid',updatedAt:new Date().toISOString()}:request));notify(['customer'],'Quote received',`${target.provider} sent a quote for ${target.service}: R${total}.`,'quote',id);return true;};
 const approveQuote=(id:string)=>{const target=requests.find(request=>request.id===id);if(!target||target.quoteStatus!=='pending_approval'||!target.pricing)return false;setRequests(current=>current.map(request=>request.id===id?{...request,status:request.status==='Pending'?'Accepted':request.status,quoteStatus:'approved',paymentStatus:'authorised',arrivalStatus:'en_route',providerDistanceKm:request.providerDistanceKm??2.4,pricing:{...request.pricing!,approvedAt:new Date().toISOString()},price:`R${request.pricing!.total}`,updatedAt:new Date().toISOString()}:request));notify(['provider','administrator'],'Quote accepted',`${target.customer||'Customer'} accepted the ${target.service} quote.`,'quote',id);return true;};
 const rejectQuote=(id:string)=>{const target=requests.find(request=>request.id===id);if(!target||target.quoteStatus!=='pending_approval')return false;setRequests(current=>current.map(request=>request.id===id?{...request,quoteStatus:'rejected',paymentStatus:'unpaid',updatedAt:new Date().toISOString()}:request));notify(['provider'],'Quote declined',`${target.customer||'Customer'} declined the ${target.service} quote.`,'quote',id);return true;};
 const cancellableStatuses:Request['status'][]=['Searching','Pending','Accepted'];
 const canCancelRequest=(request:Request)=>cancellableStatuses.includes(request.status)&&(role==='customer'||role==='provider'||role==='administrator'||role==='system-admin');
 const cancelRequest=(id:string,reason:CancellationReason='Other')=>{const target=requests.find(request=>request.id===id);if(!target||!canCancelRequest(target))return false;const fee=role==='customer'&&target.status==='Accepted'?'R50':'R0';setRequests(current=>current.map(request=>request.id===id?{...request,status:'Cancelled',cancelledAt:new Date().toISOString(),cancellationReason:reason,cancellationFee:fee,updatedAt:new Date().toISOString()}:request));notify(['customer','provider','administrator'],`Booking cancelled`,`${target.service} was cancelled. Reason: ${reason}.`,'booking',id);return true;};
 const value=useMemo<Context>(()=>({role,setRole,authenticated,login,logout,requests,addRequest,createRequest,updateRequest,verifyArrival,submitCompletionProof,submitQuote,approveQuote,rejectQuote,canCancelRequest,cancelRequest,removeRequest:id=>cancelRequest(id,'Other'),favourites,toggleFavourite:id=>setFavourites(x=>x.includes(id)?x.filter(i=>i!==id):[...x,id]),messages,sendMessage:(providerId,text,meta)=>{const provider=providerDirectory.find(item=>item.id===providerId);const recipientRoles=meta?.recipientRoles|| (role==='customer'?['provider'] as Role[]:role==='provider'?['customer'] as Role[]:role==='administrator'?['customer','provider'] as Role[]:['administrator'] as Role[]);const title=meta?.title||provider?.name||'FixMate conversation';const avatar=meta?.avatar||provider?.avatar||title.split(' ').map(x=>x[0]).join('').slice(0,2)||'FM';setMessages(x=>{const existing=x.find(m=>(meta?.requestId&&m.requestId===meta.requestId)||m.providerId===providerId);const next={providerId,provider:title,avatar,text,timestamp:'now',unread:true,requestId:meta?.requestId,senderRole:role,senderName:profile.name,recipientRoles,unreadFor:recipientRoles};if(existing)return [{...existing,...next,id:existing.id},...x.filter(m=>m.id!==existing.id)];return [{...next,id:`msg_${Date.now()}`},...x];});notify(recipientRoles,'New message',`${profile.name}: ${text}`,'message',meta?.requestId);},markMessageRead:id=>setMessages(x=>x.map(m=>m.id===id?{...m,unread:false,unreadFor:(m.unreadFor||[]).filter(item=>item!==role)}:m)),alerts,pushAlert,markAlertRead,markAllAlertsRead,reviews,addReview:r=>setReviews(x=>x.some(existing=>existing.requestId===r.requestId)?x:[r,...x]),hasReview:requestId=>reviews.some(r=>r.requestId===requestId),complaints,addComplaint:c=>{setComplaints(x=>[c,...x]);notify(['administrator','system-admin'],'New complaint',`${c.customer}: ${c.subject}`,'complaint',c.requestId);},providerDirectory,addProvider:p=>setProviderDirectory(x=>[...x,p]),serviceDirectory,addService:s=>setServiceDirectory(x=>[...x,s]),providerStatuses,setProviderStatus:(id,status)=>{setProviderStatuses(x=>({...x,[id]:status}));notify(['administrator','system-admin'],'Provider status changed',`${id} is now ${status}.`,'system');},availability,setAvailability:(id,value)=>setAvailabilityState(x=>({...x,[id]:value})),profile,updateProfile:patch=>setProfile(p=>({...p,...patch})),ready}),[role,authenticated,requests,favourites,messages,alerts,reviews,complaints,providerDirectory,serviceDirectory,providerStatuses,availability,profile,ready]);return <AppContext.Provider value={value}>{children}</AppContext.Provider>}
export const useApp=()=>useContext(AppContext);
